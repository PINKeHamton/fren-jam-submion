#define MAPSIZE 511 // 0-511 so just 512 tiles
#define TILESETSIZE 16
//               X        Y
char mapTable[MAPSIZE][MAPSIZE];
char name[5]; // EXMP: W#M# is the naming standerd and it MAXS at W##M##

typedef enum {TILES = 0, ENTITES} TileType;

void loadMap() {
  
}

